import styled from "styled-components";

export const PasswordInputContainer = styled.div`
  position: relative;
  display: flex;
  flex-direction: row;
  width: 100%;

  .eye {
    position: absolute;
    right: 4px;
    top: 8px;
  }
`;
